{% extends '::base_crud.html.twig' %}

{% block body -%}
    <h1>Edici&oacute;n de serie</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('series') }}">
            Volver al listado
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>

{% endblock %}
