{% extends '::base_crud.html.twig' %}

{% block body -%}
    <h1>Series creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('series') }}">
            Volver al listado
        </a>
    </li>
</ul>

{% endblock %}
